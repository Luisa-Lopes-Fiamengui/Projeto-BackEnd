@model IEnumerable<ProjetoBackend.Models.Servico>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center">Serviços</h1>

<p>
    <a asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValorServico)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ValorServico)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ServicoId">Editar</a>
                    <a asp-action="Delete" asp-route-id="@item.ServicoId">Deletar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
